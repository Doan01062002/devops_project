server {
    listen 80;
    server_name _;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Cấu hình này rất quan trọng với SPA như ReactJS tránh lỗi 404
        try_files $uri $uri/ /index.html;
    }
}
